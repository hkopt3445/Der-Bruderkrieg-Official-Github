UGL_KLV_the_white_army_divide = {

}

UGL_KLV_unify_russia = {
	unify_russia_formable = {
		available = {
			all_of_scopes = {
				array = global.russian_owned_core_states
				is_owned_by = UGL
			}
		}
		visible = {
			OR = {
				tag = UGL
				tag = KLV
				tag = DJI
			}
		}

		cost = 0
		days_remove = 0
		fire_only_once = yes

		remove_effect = {
			UGL = {
				set_cosmetic_tag = unified_russia ##just copy russia kek

			}
		}
	}

	
	UGL_attack_FROM = {
		available = {
		  tag = UGL
		  
		}

		visible = {
		  tag = UGL
		  if = {
			  limit = {
				  FROM = {
					has_country_flag = general_state_russian_collapse
				  }
			  }
			  has_completed_focus = UGL_the_generals
		  }
		  if = {
			limit = {
				FROM = {
				  tag = FIN
				}
			}
			has_completed_focus = UGL_the_war_for_karelia
		}
		 
		}
		target_trigger = {
			UGL = { is_neighbor_of = FROM } 
			FROM = {
				has_country_flag = is_russian_collapse_nation
			}
		}
		cost = 0
		days_remove = 0
		ai_will_do = {
		  factor = 20
		}
		remove_effect = {
		  UGL = {
			  create_wargoal = {
				  target = FROM
				  type = annex_everything
			  }
		  }
		}
	}
}